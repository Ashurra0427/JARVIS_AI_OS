# STATUS: Scaffolded, not yet implemented

This folder is empty scaffolding for a future multi-agent project-management
workflow (e.g. breaking a larger goal into tracked sub-tasks across multiple
specialist agents, with status/progress tracking). No code exists here yet.

**Intended scope (not yet designed in detail):**
- Likely the consumer of `cognition/intelligence/project_intelligence.py`
  (currently archived at `archive/legacy_project_intelligence/` — see that
  folder's README before reviving it for this pipeline)
- A defined project/task/sub-task data model, distinct from
  `GoalManager`'s single-session goal tracking

Do not assume any of the above exists. This is a placeholder so the
directory's intent is documented instead of being a bare empty folder.

(Phase 4.5)
